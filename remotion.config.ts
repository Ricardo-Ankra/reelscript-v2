// Remotion CLI / Studio config (used for local preview via `npm run studio`).
// Lambda rendering uses the bundled site, not this file.
import { Config } from '@remotion/cli/config';

Config.setVideoImageFormat('jpeg');
Config.setOverwriteOutput(true);
